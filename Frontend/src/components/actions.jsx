import { StyledActions } from "../styles/actions.styled";

export const Actions = () => {
    return (
        <StyledActions>
            <span className="dot"></span>
            <span className="dot"></span>
            <span className="dot"></span>
        </StyledActions>
    );
}
